#ifndef TRANSACTION_H
#define TRANSACTION_H

// Define a structure to hold transaction details
typedef struct {
    int amount;
    int accountType;
    int duration;
} Transaction;

#endif // TRANSACTION_H
